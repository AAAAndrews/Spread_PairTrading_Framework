# 焦炭焦煤基本面数据快速参考

## 🎯 一键运行顺序

```
单元格 1  → 初始化模块
单元格 2  → 获取期货数据（JM, J）
单元格 3  → 从数据库提取期货数据
单元格 4  → 📦 导入基本面数据（标题）
单元格 5  → 📥 执行导入 CSV → 数据库
单元格 6  → 📊 提取基本面数据（标题）
单元格 7  → 📤 数据库 → fundamental_data
单元格 8  → 获取宏观数据（可选）
单元格 9  → 🔧 创建基本面特征
单元格 10 → 📋 测试查看数据
```

## 📊 数据字段速查

| 字段名 | 含义 | 单位 |
|-------|------|------|
| `coke_warehouse_receipt` | 焦炭仓单 | - |
| `coking_coal_warehouse_receipt` | 焦煤仓单 | - |
| `bohai_port_inventory` | 港口库存 | 万吨 |
| `bohai_port_rail_inflow` | 铁路调入 | 万吨 |

## 🔑 关键修改点

在**原特征工程单元格**中添加：

```python
all_features_list = [
    spread_features,
    price_features,
    technical_features,
    macro_features,
    fundamental_features,  # 👈 加这行
    target_df[['target']]
]
```

## ⚡ 常用代码片段

### 查看基本面数据
```python
fundamental_data['COAL_INVENTORY'].tail()
```

### 查看特征列表
```python
print(fundamental_features.columns.tolist())
```

### 检查数据范围
```python
print(f"起始: {fundamental_data['COAL_INVENTORY'].index.min()}")
print(f"结束: {fundamental_data['COAL_INVENTORY'].index.max()}")
```

## 🕐 时区说明

- **基本面数据**: Asia/Shanghai (UTC+8)
- **期货数据**: 根据数据源
- **合并时**: 自动处理时区差异

## 💡 提示

- ✅ 数据已自动处理缺失值
- ✅ 时间已自动对齐
- ✅ 特征已标准化命名
- ⚠️ 记得修改特征工程代码
